-- MyPocketOS - Conky システム情報表示
-- CrunchBang系を参考にした、画面右上に常時表示するシンプルな情報パネル。
--
-- 参照: man conky(1) (conky-std 1.22.1, Debian 13 trixie)
--       /etc/conky/conky.conf (conky-std 公式デフォルト設定)
--
-- 方針:
-- - 外部コマンド実行 (exec/execi) は使用しない
-- - own_window_transparent (ARGB visualなし) によるコンポジタ不要の疑似透明背景
-- - フォントは日本語グリフを含む Noto Sans Mono CJK JP (fonts-noto-cjk) を使用
-- - 起動モード表示はConky組み込みのLua ($lua、外部プロセスを起動しない) で
--   XDG_RUNTIME_DIR配下の状態ファイルを読む。mypocketos-boot-mode.lua参照。
-- - ネットワーク使用状況 (Down/Up) は、特定インターフェース名 (wlan0/eth0等)
--   をハードコードせず、Conky組み込みのLua API `conky_parse()` から
--   ${gw_iface} (default routeのinterface名) を取得し、それを明示的に
--   ${downspeed IFACE}/${upspeed IFACE}へ渡すことで実現する
--   (2026-09-07修正。mypocketos-network.lua参照)。引数なしの
--   ${downspeed}/${upspeed} (Conky内部のデバイス自動選択に委ねる方式、
--   upstream既定のconky.confと同じ無指定形式) は、実機でdefault route
--   interfaceを正しく選ばずDown/Upが常に0Bになる不具合が確認されたため
--   採用していない。`conky_parse()`はConky自身のTEXT評価をLuaから呼び出す
--   Conky公式のLua API関数であり、exec/execiのような外部コマンド起動は
--   一切発生しない。

conky.config = {
    lua_load = os.getenv('HOME') .. '/.config/conky/mypocketos-boot-mode.lua;' ..
        os.getenv('HOME') .. '/.config/conky/mypocketos-network.lua',

    alignment = 'top_right',
    gap_x = 16,
    gap_y = 16,
    minimum_width = 220,
    minimum_height = 5,
    background = false,
    update_interval = 2.0,
    cpu_avg_samples = 2,
    net_avg_samples = 2,
    double_buffer = true,
    no_buffers = true,

    use_xft = true,
    font = 'Noto Sans Mono CJK JP:size=10',
    default_color = 'white',
    default_outline_color = 'white',
    default_shade_color = 'black',

    draw_borders = false,
    border_width = 0,
    border_inner_margin = 8,
    border_outer_margin = 0,
    draw_graph_borders = true,
    draw_outline = false,
    draw_shades = false,

    own_window = true,
    own_window_class = 'Conky',
    own_window_type = 'normal',
    own_window_hints = 'undecorated,sticky,below,skip_taskbar,skip_pager',
    own_window_transparent = true,
    own_window_argb_visual = false,

    out_to_x = true,
    out_to_console = false,
    out_to_ncurses = false,
    out_to_stderr = false,
    out_to_wayland = false,

    show_graph_range = false,
    show_graph_scale = false,
    stippled_borders = 0,
    uppercase = false,
    use_spacer = 'none',
}

conky.text = [[
${font Noto Sans Mono CJK JP:bold:size=13}MyPocketOS${font}
${color grey}${hr 1}$color
${color grey}ホスト名:$color ${alignr}${nodename}
${color grey}カーネル:$color ${alignr}${kernel}
${color grey}稼働時間:$color ${alignr}${uptime}
${color grey}起動モード:$color ${alignr}${lua mypocketos_boot_mode}
${color grey}CPU使用率:$color ${alignr}${cpu}%
${cpubar 6}
${color grey}メモリ:$color ${alignr}${mem} / ${memmax} (${memperc}%)
${membar 6}
${color grey}ルートFS (/):$color ${alignr}${fs_used /} / ${fs_size /} (${fs_used_perc /}%)
${fs_bar 6 /}
${if_match "${lua mypocketos_network_connected}" == "yes"}${color grey}ネットワーク:$color
${color grey}  Down:$color ${alignr}${lua mypocketos_network_downspeed}
${color grey}  Up:$color ${alignr}${lua mypocketos_network_upspeed}${else}${color grey}ネットワーク:$color ${alignr}未接続${endif}
${color grey}${hr 1}$color
${color grey}ショートカット$color
Super+Space  アプリメニュー
Super+T      端末
Super+E      ファイルマネージャー
Alt+F4       ウィンドウを閉じる
Alt+Tab      ウィンドウ切り替え
${color grey}${hr 1}$color
${color grey}ウィンドウスナップ$color
Super+Left   左半分
Super+Right  右半分
Super+Up     最大化
Super+Down   最大化解除
]]

# SPDX-FileCopyrightText: no
# SPDX-License-Identifier: CC0-1.0
#
# Configuration file for Calamares
# Syntax is YAML 1.2
#
# ==== MyPocketOS における位置づけ =====================================
# このファイルは、Debian公式パッケージ calamares-settings-debian が
# /etc/calamares/settings.conf として提供するファイルを、live-buildの
# config/includes.chroot 経由で上書きするMyPocketOS独自版である
# (live-buildはchroot local includesをパッケージインストール後に
# 適用するため、この配置で確実に上書きされる)。
#
# 変更点はこの1点のみ:
#   exec フェーズの `packages` モジュールを `shellprocess@livepurge`
#   へ置き換えた。
#
# 理由: Calamaresの packages モジュール(apt backend)は、
# /usr/lib/*/calamares/modules/packages/main.py の PMApt.remove() が
#     apt-get --purge -q -y remove <pkgs>
#     apt-get --purge -q -y autoremove
# を無条件に連続実行する実装になっており、config側(packages.conf)に
# autoremoveを抑止する設定項目は存在しない(Calamares本体のPythonコード
# を直接パッチしない限り無効化できない)。この2行目のautoremoveが、
# Live専用パッケージ除去後に sudo・user-setup 等を「不要」と誤判定して
# 削除しようとし、sudoのpre-removalスクリプトが安全装置として
# インストールを失敗させる原因になった
# (reports/ai-review/20260913-calamares-spike-autoremove-fix.md 参照)。
#
# 対策として、標準の `packages` モジュール(と、それが読む
# /etc/calamares/modules/packages.conf、calamares-settings-debianが
# 設置したもの、未変更のまま残置)は exec シーケンスから外し、
# 代わりにCalamares公式の shellprocess モジュール(calamaresパッケージに
# 標準搭載、追加パッケージ不要)のインスタンスで、Live専用パッケージのみを
# 明示的にapt-get purgeする。autoremoveは一切呼ばない。詳細は
# /etc/calamares/modules/shellprocess-livepurge.conf を参照。
#
# 保守上の注意: 上記1点の変更以外は、calamares-settings-debianが
# 提供するsettings.confの内容をそのまま維持している。将来
# calamares-settings-debianパッケージが更新され、このファイルの
# シーケンス・既定値が変わった場合、このMyPocketOS版は自動的には
# 追従しない。パッケージ更新時は /usr/share/doc/calamares-settings-debian/
# またはsalsa.debian.org/live-team/calamares-settings-debianの最新版と
# 差分を取り、必要であれば本ファイルを更新すること。
# =======================================================================
---

# "local" is LIBDIR/calamares/modules with settings in SHARE/calamares/modules
modules-search: [ local, /usr/lib/calamares/modules ]

# ==== MyPocketOS独自追加: shellprocessモジュールのインスタンス定義 =====
# id: livepurge というインスタンス名で shellprocess モジュールを登録する。
# 実際のコマンドは config で指定した
# /etc/calamares/modules/shellprocess-livepurge.conf に書く。
# sequence内では "shellprocess@livepurge" という instance key で参照する。
instances:
- id:     livepurge
  module: shellprocess
  config: shellprocess-livepurge.conf
# =======================================================================

# If this is set to true, Calamares refers to itself as a "setup program"
# rather than an "installer". Defaults to the value of dont-chroot, but
# Calamares will complain if this is not explicitly set.
oem-setup: false

# If this is set to true, the "Cancel" button will be disabled entirely.
# The button is also hidden from view.
#
# This can be useful if when e.g. Calamares is used as a post-install
# configuration tool and you require the user to go through all the
# configuration steps.
#
# Default is false, but Calamares will complain if this is not explicitly set.
#
# YAML: boolean.
disable-cancel: false

# If this is set to true, the "Cancel" button will be disabled once
# you start the 'Installation', meaning there won't be a way to cancel
# the Installation until it has finished or installation has failed.
#
# Default is false, but Calamares will complain if this is not explicitly set.
#
# YAML: boolean.
disable-cancel-during-exec: false

# If this is set to true, then once the end of the sequence has
# been reached, the quit (done) button is clicked automatically
# and Calamares will close. Default is false: the user will see
# that the end of installation has been reached, and that things are ok.
#
#
quit-at-end: false

sequence:
# Phase 1 - prepare.
# View modules are shown as UI pages, jobs from job modules
# are executed immediately in the background.
# Jobs should be executed sparingly (if at all) in this phase.
- show:
  - welcome
  - locale
  - keyboard
  - partition
  - users
  - summary

# Phase 2 - install.
# View modules are not shown. Only the view modules shown
# in the previous phase are allowed, their names should be
# added here as placeholders to specify the order in which
# view module jobs should be enqueued. Job modules are
# also allowed.
- exec:
  - partition
  - mount
  - unpackfs
  - luksbootkeyfile
  - dpkg-unsafe-io
  - sources-media
  - machineid
  - fstab
  - locale
  - keyboard
  - localecfg
  - users
  - displaymanager
  - networkcfg
  - hwclock
  - services-systemd
  - bootloader-config
  - grubcfg
  - bootloader
  # MyPocketOS: 標準の `packages` (apt purge + 無条件autoremove) から
  # `shellprocess@livepurge` (Live専用パッケージのみ明示purge、
  # autoremoveなし) へ置き換え。位置は元のままinitramfs再構成の前に置く
  # (live-boot除去がinitramfsフックへ影響するため)。
  - shellprocess@livepurge
  - plymouthcfg
  - initramfscfg
  - initramfs
  - dpkg-unsafe-io-undo
  - sources-media-unmount
  - sources-final
  - umount

# Phase 3 - postinstall.
# View modules are shown as UI pages, jobs from job modules are
# executed immediately in the background.
# Jobs should be executed sparingly (if at all) in this phase.
- show:
  - finished

# A branding component is a directory, either in
# SHARE/calamares/branding or in /etc/calamares/branding
# (the latter takes precedence). The directory must contain a
# YAML file branding.desc which may reference additional resources
# (such as images) as paths relative to the current directory.
# Only the name of the branding component (directory) should be
# specified here, Calamares then takes care of finding it and
# loading the contents.
branding: mypocketos

# If this is set to true, Calamares will show an "Are you sure?" prompt right
# before each execution phase, i.e. at points of no return. If this is set to
# false, no prompt is shown. Default is false.
#
# YAML: boolean.
prompt-install: false

# If this is set to true, Calamares will execute all target environment
# commands in the current environment, without chroot. This setting should
# only be used when setting up Calamares as a post-install configuration tool,
# as opposed to a full operating system installer.
#
# Some official Calamares modules are not expected to function with this
# setting. (e.g. partitioning seems like a bad idea, since that is expected to
# have been done already)
#
# Default is false (for a normal installer).
#
# YAML: boolean.
dont-chroot: false

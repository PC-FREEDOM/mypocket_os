source /boot/grub/config.cfg

# MyPocketOS - UEFI/GRUB 起動メニュー
#
# Debian live-build標準テンプレートを基にした。
# 通常Live、永続Live、Fail-safeを明示的に定義する。
# 置換トークンの詳細は/usr/lib/live/build/binary_grub_cfgで確認済み。

# Live boot
menuentry "MyPocketOS Live" {
	linux	@KERNEL_LIVE@ @APPEND_LIVE@ nopersistence
	initrd	@INITRD_LIVE@
}

menuentry "MyPocketOS Live (Persistence)" {
	linux	@KERNEL_LIVE@ @APPEND_LIVE@ persistence
	initrd	@INITRD_LIVE@
}

menuentry "MyPocketOS Live (Fail-safe)" {
	linux	@KERNEL_LIVE@ @LB_BOOTAPPEND_LIVE_FAILSAFE@
	initrd	@INITRD_LIVE@
}

# Installer (if any)
if @ENABLE_INSTALL_MENU@; then

source	/boot/grub/install_start.cfg

submenu 'Advanced install options ...' --hotkey=a {

	source /boot/grub/theme.cfg

	source	/boot/grub/install.cfg

}
fi

submenu 'Utilities...' --hotkey=u {

	source /boot/grub/theme.cfg

	# Memtest (if any)
	if @ENABLE_MEMTEST@; then
		source /boot/grub/memtest.cfg
	fi

	# Firmware setup (UEFI)
	if [ "${grub_platform}" = "efi" ]; then
		menuentry "UEFI Firmware Settings" --hotkey=f {
			fwsetup
		}
	fi

	# Verify the checksums
	if @ENABLE_VERIFY_CHECKSUMS@; then
		menuentry "Verify integrity of the boot medium" --hotkey=v {
			linux	@KERNEL_LIVE@ @APPEND_VERIFY_CHECKSUMS@
			initrd	@INITRD_LIVE@
		}
	fi
}

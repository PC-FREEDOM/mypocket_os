# MyPocketOS Known Issues

この文書は、MyPocketOS 初回リリース時点で確認されている制約・注意事項をまとめたものです。

## インストール

### 正式サポートは「専用ディスクへの消去インストール」のみ

Calamaresによる通常インストールでは、初回リリースの正式サポート範囲を
**インストール先ディスク全体を初期化する「ディスクの消去」方式**としています。

Calamares上に表示される以下の方式は、MyPocketOSでは十分な検証を行っていません。

- 既存OSとの共存インストール
- Dual Boot
- 既存OSの置き換え
- 手動パーティション設定

これらが必ず動作しないという意味ではありませんが、初回リリースでは正式サポート対象外です。

### インターネット接続が必要

Calamaresによる通常インストールにはインターネット接続が必要です。

Wi-Fiを使用する場合は、**Calamaresを起動する前にネットワークへ接続してください。**
Calamares起動後に接続しても、インターネット接続の要件表示が自動的に再評価されない場合があります。

### インストール要件

Calamaresによる通常インストールには、以下を目安とします。

- インストール先ストレージ: 16 GiB以上
- RAM: 約2 GiB以上

## Persistence

### Persistence領域は暗号化されない

初回リリースのPersistence領域は暗号化されません。

Persistenceへ保存した `/home` のデータに加え、Wi-Fi設定を保持した場合は
NetworkManagerの接続情報も保存されます。

USBメモリを紛失・盗難された場合、保存内容を第三者に読み取られる可能性があります。

### 一部のPersistence経路・環境は未検証

初回リリースでは主要なPersistence経路を確認していますが、
すべての組み合わせを検証しているわけではありません。

特に以下は検証範囲が限定されています。

- Secure Boot環境でのWi-Fi Persistence
- Mode AのUEFI / Secure Boot実機
- 複数Wi-Fiプロファイル
- 複数機種・複数ファームウェアでの動作

## 起動・終了時の表示

### shutdown時に警告が表示される場合がある

Live環境の終了時に、以下のようなメッセージが表示される場合があります。

- `Device or resource busy`
- `Could not detach loopback`
- watchdog関連の警告

確認済みの環境では、USB取り外し案内、再起動、インストール後のシステム起動に
実害は確認されていません。

## ハードウェア互換性

### 一部のIntel内蔵グラフィックス環境で表示乱れが発生する場合がある

確認した一部のIntel内蔵グラフィックス搭載機では、通常起動時に
縦線などの表示乱れが発生する場合があります。

この現象をすべてのIntel GPU環境へ一般化するものではありません。
回避策としてカーネルパラメータの追加が必要になる場合があります。

## その他

### jgmenuの日本語検索には制約がある

jgmenuの検索では、日本語入力・検索の挙動に制約が見られる場合があります。

### タイムゾーンのGeoIP自動選択には非対応

初回リリースでは、GeoIPによるタイムゾーンの自動選択には対応していません。
既定値は `Asia/Tokyo` です。

### 一部環境でjournaldのWATCHDOG警告が表示される場合がある

一部の終了処理等でWATCHDOG関連の警告が表示される場合があります。
現時点では、通常利用への実害は確認されていません。

---

確認範囲は初回リリース時点のものです。
ハードウェアやファームウェアの組み合わせによって、結果が異なる場合があります。

# Third-Party Notices

MyPocketOSには、第三者が開発・公開しているソフトウェア、ライブラリ、
アイコンテーマ、その他のアセットが含まれています。

それぞれのコンポーネントには、各プロジェクトが定めるライセンス条件が適用されます。
MyPocketOS全体の `LICENSE` が、これら第三者コンポーネントの個別ライセンスを
上書きするものではありません。

## MyPocketOS-Fluent-yellow icon theme

MyPocketOSで使用する `MyPocketOS-Fluent-yellow` アイコンテーマには、
同梱アーカイブ内の `COPYING` にGNU General Public License Version 3
(GPLv3)が含まれています。

ライセンス文書は以下に同梱されています。

`config/includes.chroot/usr/share/mypocketos/icon-themes/MyPocketOS-Fluent-yellow.tar.gz`

アーカイブ内:

`MyPocketOS-Fluent-yellow/COPYING`

## Debian packages and bundled software

MyPocketOSはDebian 13をベースとしており、多数のDebianパッケージおよび
オープンソースソフトウェアを利用しています。

これらのパッケージには、それぞれ個別のライセンスが適用されます。
インストール済みシステムでは、各パッケージのライセンス情報を通常
`/usr/share/doc/<package>/copyright`
などから確認できます。

主なコンポーネントには、以下が含まれます。

- Debian GNU/Linux
- Linux kernel
- Openbox
- tint2
- jgmenu
- PCManFM
- Calamares
- Flatpak
- IBus
- Mozc
- NetworkManager

上記は主要コンポーネントの例であり、完全な一覧ではありません。

各ソフトウェアの著作権およびライセンス条件については、
それぞれの配布元および同梱ライセンス文書を参照してください。
